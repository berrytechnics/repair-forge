export async function register() {
  // Sentry is initialized via sentry.server.config.ts and sentry.edge.config.ts
  // This hook is called by Next.js automatically when instrumentationHook is enabled
  // The config files handle initialization, so we don't need to do anything here
  // unless we need custom initialization logic
}

